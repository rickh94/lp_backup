language: python
cache: pip
python:
  - "3.6"
  - "3.7-dev"

before_install:
#  - pip install pipenv
  - pip install codecov
  - pip install pytest-cov
  - pip install pytest
  - pip install -r requirements.txt
install:
  - pip install .

script: pipenv run pytest

after_success:
  - codecov
